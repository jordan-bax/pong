{
    "name": "game-service",
    "version": "1.0.0",
    "description": "pong microservice for transcendence",
    "scripts": {
      "build": "tsc --build",
      "start": "node dist/game.js"
    },
    "dependencies": {
      "@fastify/cookie": "^11.0.2", // user side cookies
      "@fastify/session": "^11.1.0", // server side cookies/session
      "@fastify/static": "^8.1.1", // serve static website
      "@types/node": "^22.15.17", // node.js
      "bcryptjs": "^2.4.3", // hashing
      "fastify": "^5.3.2", // fastify backed
      "sqlite": "^5.1.1", // database
      "sqlite3": "^5.1.7" // database
    },
    "devDependencies": {
      "@types/bcryptjs": "^2.4.3", // secfic version of bcrypt
      "typescript": "^5.0.0" // typescript
    }
  }